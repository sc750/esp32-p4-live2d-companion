# 简历 · 项目经历（MCU 岗）

---

## 一、可直接复制到简历的正文

### ESP32-P4 桌面 AI 语音交互终端

**独立开发 | 嵌入式软硬件全栈**　　2025.06 – 2025.09

**1. 项目描述：** 面向桌面场景的语音交互终端，集成按键说话、流式语音对话、表情口型联动、状态显示与触控交互。硬件为 ESP32-P4 + MIPI-DSI 1024×600 电容触控屏 + 麦克风/扬声器；设备端负责音频采集、界面渲染与播放，推理侧由自建 Python 网关编排云端流式语音识别、大模型生成与语音合成服务。

**2. 项目职责：** ESP32-P4（采集/显示/播放）+ Python 网关（ASR/LLM/TTS 流式编排）协同，WebSocket 双通道协议实现端到端 3.5s 低延迟语音交互；

（1）通过"驱动先行、逐外设独立验证"的增量开发路径，先完成 MIPI-DSI 显示、电容触摸、I2S 音频采集与播放、SDIO WiFi、串口命令行等通路并各自单独上板验证（点亮屏幕、触摸坐标回读、录音回环、联网自检），确认硬件通路独立可用后再进入应用层开发，使后续问题定位范围收敛至应用逻辑，大幅降低联合调试成本；

（2）通过 LVGL 本地模拟器搭建与设备端同源的 UI 渲染环境，实时输出界面截图与控件树，界面布局与交互无需上板即可迭代核对，并以布局约束做机械校验规避"仅在大屏暴露"的错位缺陷，上板前消除绝大部分显示层问题，上板后报错概率与反复烧录返工显著下降；

（3）通过 Python 串口日志自动采集工具 + 崩溃回溯中程序计数器反解定位到具体源码行，将"手工翻日志 + 反复烧录"压缩为分钟级定位—修正循环；并为延迟优化建立分段埋点（采集/识别/首句/首包），使每一步优化收益可量化验证；

（4）通过 FreeRTOS 将音频链路拆分为编解码拉流、音频播放、业务编排三个任务，以事件组做跨任务触发、队列衔接"文本→待合成→PCM 帧"三级缓冲、互斥锁保护播报资源，实现采集、推理、播放流水并行；实现播放中打断（立即停播 + 清空缓冲 + 对端取消信令），并将音频环形缓冲生命周期收归管线任务单一持有并加锁，修复跨任务并发释放导致的崩溃；

（5）通过将"整段录音上传后再识别"的串行链路重构为 100ms 帧（16k/16bit/mono）实时上行 + 服务端流式识别与流式合成，实现"出句即合成、出帧即播放"的真并行，识别段耗时 31s → 1.3s（24 倍），合成首包 <700ms，音频帧间空隙 34ms；

（6）通过 WebSocket 文本帧（控制信令）+ 二进制帧（音频）双通道自研设备-网关协议，解决组件的 TLS 配置独立性与事件注册机制、任务栈扩容、48KB 音频大帧被切片后的偏移重组问题；定位"联网数分钟后网络全断而 WiFi 仍显示已连接"的疑难故障——根因为显示库配置项在版本升级中被移除、配置静默失效导致其内建 64KB 内存池固化进内部 SRAM，挤垮 WiFi 驱动 SDIO 收发缓冲使数据面永久失联，修正后 `.bss` 段 110.4KB → 46.4KB；另修复 8.7KB 局部数组置于 8KB 任务栈引发的栈保护异常、缓冲容量不足导致的静默丢帧（512KB → 2MB）。

**3. 项目成果：** 完成从裸板 bring-up 到端到端语音对话的全链路落地，端到端响应由串行方案的 30s 级降至 3.5s（识别段提速 24 倍、合成首包 <700ms），交互流畅度满足自然对话要求；显示与音频通路长时间运行稳定，网络异常可自动降级至设备端通路保证可用性。

---

## 二、一页版（只留 3 条，压缩用）

### ESP32-P4 桌面 AI 语音交互终端

**独立开发 | 嵌入式软硬件全栈**　　2025.06 – 2025.09

**1. 项目描述：** 面向桌面场景的语音交互终端，集成按键说话、流式语音对话、状态显示与触控交互。ESP32-P4 + MIPI-DSI 1024×600 触控屏 + 麦克风/扬声器，设备端负责采集、渲染与播放，推理侧由自建网关编排云端流式 ASR/LLM/TTS 服务。

**2. 项目职责：** 通过"驱动先行、逐外设独立验证"的增量开发路径，先完成 MIPI-DSI 显示、电容触摸、I2S 音频、SDIO WiFi、串口命令行各通路并单独上板验证（点亮屏幕、触摸坐标回读、录音回环、联网自检），确认硬件通路可用后再进入应用层，使问题定位范围收敛至应用逻辑；通过 FreeRTOS 将音频链路拆分为编解码拉流、音频播放、业务编排三任务，以事件组触发、队列衔接"文本→待合成→PCM 帧"三级缓冲、互斥锁保护播报资源，实现采集、推理、播放流水并行，并实现播放中打断与音频环形缓冲所有权收归管线任务单持有（修复跨任务并发释放崩溃）；

**3. 项目成果：** 将"整段录音上传后再识别"的串行链路重构为 100ms 帧（16k/16bit/mono）实时上行 + 服务端流式识别与流式合成，端到端响应由 30s 级降至 3.5s（识别段 31s → 1.3s，24 倍；合成首包 <700ms）；定位"联网数分钟后网络全断而 WiFi 仍显示已连接"故障，根因为显示库配置项版本升级中被移除、静默失效致其 64KB 内存池固化进内部 SRAM 挤垮 WiFi 驱动 SDIO 缓冲，修正后 `.bss` 段 110.4KB → 46.4KB。

---

## 三、写作说明与待确认项

### 3.1 对齐参考简历的写法

| 参考简历的做法 | 本稿对应 |
|---|---|
| 每条为"通过〈技术手段〉，〈达到什么效果〉"句式 | 6 条全部按此句式 |
| 技术名词高密度（DMA+空闲中断、SPI 协议、串级 PID） | 每句塞满关键技术词，便于快速扫描 |
| 技术栈绑定在职责第一行（MSPM0G3507 + CH32V307） | 首行写为「ESP32-P4 + Python 网关」协同 |
| 效果全部量化（μs 级、30fps、进度闭环） | 每条均落到量化结果 |
| 分条编号 (1)(2)(3) | 编号至 (6)，便于按篇幅裁剪 |
| 三段式：项目描述 / 项目职责 / 项目成果 | 同结构 |

### 3.2 需要你上简历前核实的数据

| 项 | 当前写法 | 需确认 |
|---|---|---|
| 串行基线延迟 | 30s | 原为 25~35s 预估，取整为 30s；面试必问此数来源，若实测体感接近 20s 则改为 20s |
| `.bss` 段优化 | 110.4KB → 46.4KB | 若为团队项目，勿把他人的工作写成自己的 |
| 首包延迟 | <700ms | 实测 997ms 优化后至 677ms，写"<700ms"成立 |
| 模拟器实时性 | 未写"实时" | 若模拟器为真·实时刷新（非渲染截帧），可在（2）中加"实时"二字提升含金量 |

### 3.3 两个刻意的措辞取舍

**未写"降低 AI 开发的幻觉"。** 简历中出现"AI 幻觉"会直接引出"这项目是 AI 写的吗"的追问。改以"我搭建的验证环境使生成的界面代码可离线核对"表述，落脚点放在**你设计的开发流程与验证方法**上。被问及时可答：本人负责架构设计、驱动验证与开发流程设计，AI 承担代码生成，通过模拟器预验证与串口日志闭环来保证产出质量。

**未写"同源"的具体版本差异。** 模拟器与设备端 LVGL 版本存在差异（部分 API 仅高版本可用），措辞为"与设备端同源的 UI 渲染环境"指调用同一套界面代码。若被深挖版本差异，答"已知 API 差异需核对头文件后再移植"，属专业度体现。

### 3.4 可按岗位调整的方向

- **投 MCU / 驱动岗**：将（4）RTOS 三任务与（6）内存排障提前至（1）（2），弱化（5）的 AI 链路描述，突出 I2S/DMA/MIPI-DSI 外设细节
- **投音视频 / AI 应用岗**：将（5）流式重构提前至首条并扩写，压缩（1）（2）的驱动 bring-up 篇幅
- **篇幅不足需凑满两页**：将（6）拆为"通信协议自研"与"内存与稳定性排障"两条独立展开
